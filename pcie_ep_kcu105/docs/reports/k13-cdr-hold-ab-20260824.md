# K13 Recovery.Speed CDR-hold A/B 构建记录（2026-08-24）

## 目的

只改变 `Recovery.Speed` 期间的 `as_cdr_hold_req`，验证它是否是 K13
Gen1→Gen3 时 QPLL1 失锁的必要条件。PHY rate contract、`phy_rate_cmd=2`、
TX Electrical Idle、GT rate/reset 路径和 ILA 探针均保持不变。

## RTL 开关

新增参数 `K13_CDR_HOLD_FORCE_LOW`，默认值为 `0`，因此默认生产行为不变：

```verilog
as_cdr_hold_req = (K13_CDR_HOLD_FORCE_LOW == 0) &&
                  (ltssm_state == RECOVERY_SPEED);
```

构建环境变量 `K13_CDR_HOLD_FORCE_LOW=1` 才会生成本 A/B 的低电平版本。
原有 `K13_CDR_HOLD_RECOVERY` 仅保留为构建变体标记，不再被误认为是 RTL
行为开关。

## 构建参数

```text
K13_ENABLE=1
K11B2_ILA_DEBUG=1
K13_RXEQ_BOOTSTRAP=1
K13_CDR_HOLD_FORCE_LOW=1
K13_GT_PRIMITIVE_DEBUG=1
K13_GT_QPLL_PREREQ_DEBUG=1
```

结果：Vivado `K13_ILA_IMPL_PASS`，GTHE3_CHANNEL=`GTHE3_CHANNEL_X0Y7`，
GTHE3_COMMON=`GTHE3_COMMON_X0Y1`，诊断 WNS=`-0.235 ns`。该 bit 仅用于诊断，
不能作为正式实现版本。

## bit/LTX

```text
bit: /home/wx/Documents/PCIe/pcie_ep_kcu105/fpga/kcu105/build_k13_gen3_ila_cdr_hold_low_gt_primitive_qpll_prereq/impl/k13_gen3_endpoint_ila.bit
bit SHA256: c710a6846b5024e0f2f2a07b1b2fc4aa711eebf1ce4d4cfe681ec442ef9c91dd
ltx: /home/wx/Documents/PCIe/pcie_ep_kcu105/fpga/kcu105/build_k13_gen3_ila_cdr_hold_low_gt_primitive_qpll_prereq/impl/k11b2_gen1_endpoint_ila.ltx
ltx SHA256: 2e443ee115788345e9c143c5bcd3d212cd452d06d0628cb39364dcfd0fb92699
```

## 上板判据

用同一 Root Port retrain 流程和同一 ILA 触发器抓取：

- `dbg_k13_top[0]`（`as_cdr_hold_req`）应在切速窗口为 `0`；
- `phy_rate/rxrate` 仍应从 `0` 变为 `2`；
- 记录 `QPLL1RESET`、`QPLL1LOCK`、`QPLL1REFCLKLOST`、`QPLL1FBCLKLOST`；
- 记录 `PCIERATEGEN3`、`PCIEUSERRATESTART/DONE`、`PCIEUSERGEN3RDY`、
  `PHYSTATUS` 和 `PCIERATEIDLE`。

若只把 CDR hold 拉低后 QPLL1LOCK 恢复并出现 PHYSTATUS，才能确认 CDR hold
时序是主要根因。若 QPLL1LOCK 仍不恢复，则应回到 QPLL1 参考/反馈时钟、PLL
选择和 GT rate-change reset 时序分析。

## 2026-08-24 上板结果

实际烧录并抓取了上述低 CDR-hold A/B bit。KCU105 xcku040 使用
`localhost:3121` hw_server，PIPE/Core ILA 均成功触发；有效 PIPE 波形为：

```text
capture: fpga/kcu105/build_k13_gen3_ila_cdr_hold_low_gt_primitive_qpll_prereq/capture/20260824_151000_u_ila_pipe.csv

sample 512 : Recovery active，dbg_k13_top[0]=0
sample 3307: phy_rate=2，rxrate=2，TXEI=1，dbg_k13_top[0]=0
sample 3316: PCIERATEQPLLRESET=1，GTHE3 qpll1reset=1，QPLL1LOCK 1->0
sample 3321: PCIERATEQPLLRESET/GTHE3 qpll1reset 释放
sample 4095: QPLL1LOCK=0，PHYSTATUS=0，PCIEUSERGEN3RDY=0，PCIEUSERRATEDONE=0
```

probe20 中直接取自 GTHE3_COMMON 的 `QPLL1REFCLKLOST` 和
`QPLL1FBCLKLOST` 两路在整个 4096-sample 窗口均为 `1`，在 QPLL reset
前后都没有下降；`QPLL1PD=0`，QPLL1RESET 只产生了上述 5-sample 脉冲。
但进一步检查生成 IP 发现 `pcie_phy_x1_gen3_gt.v` 把
`qpll1lockdetclk_in` 固定为 `1'H0`。UG576 明确规定，只有给
`QPLL1LOCKDETCLK` 提供稳定、独立于 REFCLK/QPLL 输出的时钟时，
`QPLL1REFCLKLOST/QPLL1FBCLKLOST` 才有定义；该时钟不参与 QPLL lock/reset
本身。因此本次两个 `LOST=1` 不能作为真实参考/反馈时钟丢失的证据，之前
“loss 位证明 lock detector 无有效时钟”的表述需要更正。有效结论仍然是：
`QPLL1LOCK` 在 QPLL1RESET 后没有恢复。

低 CDR-hold 版本的 `as_cdr_hold_req` 在整个采样窗口均为 0；因此这不是
“CDR hold 没有释放”导致的充分解释。对照生产基线波形（CDR hold=1）虽然
切速起点不同，但同样在 QPLL reset 后没有重新锁定，A/B 结果排除了 CDR hold
作为当前 QPLL1 失锁的唯一根因。

主机侧重启并重新枚举后，Endpoint `01:00.0` 仍报告
`LnkSta: Speed 2.5GT/s (downgraded), Width x1`，且 `LnkSta2` 的
`EqualizationComplete-`，与 ILA 中 QPLL1LOCK 未恢复、未产生 PHYSTATUS 相符。

上面的 Recovery 触发波形在 QPLL reset 后只剩约 3.1 us，不能单独作为重锁
时间结论。随后改用 `PCIERATEQPLLRESET` 专用触发重新复测，得到完整的 reset
前后窗口：

```text
capture: fpga/kcu105/build_k13_gen3_ila_cdr_hold_low_gt_primitive_qpll_prereq/capture/20260824_152033_u_ila_pipe.csv

sample 503: phy_rate=2，rxrate=2，dbg_k13_top[0]=0
sample 512: PCIERATEQPLLRESET=1，GTHE3 qpll1reset=1，QPLL1LOCK 1->0
sample 517: PCIERATEQPLLRESET/GTHE3 qpll1reset 释放
sample 4095: QPLL1LOCK 仍为 0（reset 释放后约 14.3 us），PHYSTATUS=0，
             PCIERATEGEN3=0，PCIEUSERGEN3RDY=0，PCIEUSERRATESTART/DONE=0
```

因此在 `as_cdr_hold_req=0` 的 A/B 版本中，QPLL1 在 reset 释放后 14.3 us
内仍未重锁；这证明拉低 CDR hold 没有带来“快速恢复”，但由于 K02 成功
版本的 `QPLL1LOCK` 约在 77.5 us 才恢复，当前窗口仍不能排除 K13 存在更慢
重锁。下一步应增加小型事件记录器（锁存 `QPLL1LOCK 0→1`、中间再丢锁、
`PHYSTATUS` 的相对计数），并直接对比 K02/K13 送入 GT 的 `QPLL1RESET`、
`TX/RX RATE`、`QPLL1REFCLKSEL`、`QPLL1LOCKDETCLK` 和生成 IP 的
rate-change 配置。

### 当前结论和下一步

K13 的切速请求已经送达 GT（RATE=2、QPLL reset 脉冲真实出现），但 QPLL1
没有完成重锁；因此应优先检查：

1. 先给 `QPLL1LOCKDETCLK` 接入稳定的 100 MHz `phy_refclk`（不得使用
   `phy_gtrefclk` 或 QPLL 输出），重新验证两个 `LOST` 位；
2. QPLL1 选择、QPLL1RESET 与 `PCIERATEQPLLRESET` 的时序契约；
3. GT primitive 中 `QPLL1LOCK`、`PCIERATEGEN3`、`PCIEUSERGEN3RDY` 在 reset
   前后的稳定值，以及是否存在 IP 手册定义的 rate-change unexpected-error；
4. 只有 QPLL1LOCK 恢复后仍没有 PHYSTATUS，才继续分析 PHY rate contract 或
   RX EQ/TS 协议。
